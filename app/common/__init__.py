"""
GrantThrive — Common Utilities Package
=======================================
Shared helpers used across all blueprints.

Modules:
  decorators  — JWT-based auth decorators (token_required, role_required)
  formatters  — Display helpers for currency, dates, status labels
  validators  — Input validation helpers for email, phone, dates, files
  pagination  — SQLAlchemy query pagination helper
"""
